# Top-level deployment .env. Используется docker-compose.yaml для
# выставления POSTGRES_* в контейнере БД. Все остальные настройки
# (JWT, SMTP, BUILDS_*) лежат в MavixServer/.env и MavixWeb/.env.

POSTGRES_USER=mavix
POSTGRES_PASSWORD=changeme_in_production
POSTGRES_DB=mavix
